import Panel from './components/Panel';
const App = () => {
	return (
		<>
			<Panel />

			{/* <CodeEditor onChange={handleSubmit as any} />
			<textarea defaultValue={code} />
		<Preview code={code} /> */}
		</>
	);
};

export default App;
